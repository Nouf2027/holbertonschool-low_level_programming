#I wrote a script that runs a C file through the preprocessor.  
The name of the file comes from the variable $CFILE, and the output is saved into a file called c.  
This helped me understand how the preprocessor works before compilation.
